{
  "name": "@agentparty/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agentparty/shared": "workspace:*",
    "chanfana": "^2.8.1",
    "hono": "^4.8.3",
    "partyserver": "^0.0.72"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.44",
    "typescript": "^5.8.3",
    "vitest": "~3.2.4",
    "wrangler": "^4.22.0"
  }
}
